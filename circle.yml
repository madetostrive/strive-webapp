machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - pip install awsebcli

deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy strive-webapp-dev
      - pip install -r app/requirements.txt
      - flask run
